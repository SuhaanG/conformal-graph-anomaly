# Focused revision, 23 September 2026

Purpose: address both external critiques with evidence, not acceptance forecasts.

1. Freeze and run a cached-score dose-response protocol. Compare removal by observed neighbor exposure with size-matched random removal on both existing graphs, both supervised scorers, every training seed and split, and three label-revelation budgets. No test labels enter selection. Preserve negative results and separate finite-graph replication from external generality.
2. Explain the random-sampling baseline using conditional exchangeability and cite AdaDetect, Gazin et al., and conformal link prediction. State the sampling, training, and tie assumptions explicitly. Do not import a guarantee into a selection-dependent sample-size experiment without checking those assumptions.
3. Rebuild the first page around useful accurate scores, a matched selection diagnostic, and an established fixed-batch certification option. Add an FDP/power headline figure and a filter-strength figure. Do not describe the exposure filter as established practice without evidence.
4. Remove invalid historical result tables and obsolete implementation history from the submission narrative, retaining a local archive. Keep current negative results and substantive limitations. Compress weighting and contamination into supporting analysis.
5. Fix text/table inconsistencies, document the upstream Weibo label-count discrepancy, and report actual audit validation results. Keep the AI statement honest about authors' remaining review responsibility.
6. Compile with the unchanged official 2027 style, inspect rendered pages, check the eight-page main-text limit, regenerate anonymous source/reproducibility archives, and document cloud synchronization status accurately.

Conditional follow-up: an exposure-stratified correction requires a verified conditional sampling argument; it will not be advertised as a new method merely because a reviewer suggested it. Additional graph training is secondary to resolving the realistic-selection question and is not needed for the cached-score sweep.

Completion and results are recorded in REVISION_RESULTS_20260923.md.
